import Box from "@mui/material/Box";

export default function Home() {
    return (
        <Box sx={{ display: "flex", flex: 1, justifyContent: "center", mt: 5 }}>
            Select a resource from the list to see details
        </Box>
    );
}
